use std::env;
use std::fs;
use std::io::{self, BufRead};

fn main() {
    // // Get the directory path from command-line argument
    // let args: Vec<String> = env::args().collect();
    // if args.len() < 2 {
    //     println!("Please provide a directory path as a command-line argument.");
    //     return;
    // }
    // let directory_path = &args[1];

    // // Recursively search the directory for files containing "TODO"
    // search_directory(directory_path);
}